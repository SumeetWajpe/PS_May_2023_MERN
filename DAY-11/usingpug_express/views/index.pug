extends layout

block script 
  script(src="/javascripts/script.js")

block content
  h1 Total courses : #{courses.length}
  ul(class="list-group") 
    each course in courses 
      li(class="list-group-item") #{course.title}
  
